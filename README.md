# py-ssg
> A static site generator built in Python.

# Usage
To use, you'll need to have Python 3 installed. You can start the program by running the main.sh script and run all the built in tests by running the test.sh script

# Goals
I'd love to have this dockerized so that it could be deployed other places as well.